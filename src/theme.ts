import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    // secondary: {
    //   main: '#adadad',
    // },
    // background: {
    //   paper: '#303030',
    // },
    primary: {
      main: '#FFC400',
    },
  },
})

export default theme
