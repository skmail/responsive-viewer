import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    type: 'dark',
    secondary: {
      main: '#adadad',
    },
    background: {
      paper: '#303030',
    },
    primary: {
      main: '#FFC400',
    },
  },
})
export default theme
